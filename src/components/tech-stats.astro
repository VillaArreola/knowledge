<!-- Technologies & Stats Section -->
<div class="tech-stats-section">
    <div class="tech-grid">
        <h3>💻 Tecnologías</h3>
        <div class="tech-icons">
            <div class="tech-item" title="Python">
                <div class="tech-icon python">🐍</div>
                <span class="tech-name">Python</span>
            </div>
            <div class="tech-item" title="JavaScript">
                <div class="tech-icon javascript">⚡</div>
                <span class="tech-name">JavaScript</span>
            </div>
            <div class="tech-item" title="Docker">
                <div class="tech-icon docker">🐳</div>
                <span class="tech-name">Docker</span>
            </div>
            <div class="tech-item" title="Linux">
                <div class="tech-icon linux">🐧</div>
                <span class="tech-name">Linux</span>
            </div>
            <div class="tech-item" title="AWS">
                <div class="tech-icon aws">☁️</div>
                <span class="tech-name">AWS</span>
            </div>
            <div class="tech-item" title="Kubernetes">
                <div class="tech-icon k8s">⚙️</div>
                <span class="tech-name">K8s</span>
            </div>
            <div class="tech-item" title="Git">
                <div class="tech-icon git">📚</div>
                <span class="tech-name">Git</span>
            </div>
            <div class="tech-item" title="SQL">
                <div class="tech-icon sql">🗃️</div>
                <span class="tech-name">SQL</span>
            </div>
            <div class="tech-item" title="Astro">
                <div class="tech-icon astro">🚀</div>
                <span class="tech-name">Astro</span>
            </div>
            <div class="tech-item" title="Tailwind">
                <div class="tech-icon tailwind">🎨</div>
                <span class="tech-name">Tailwind</span>
            </div>
        </div>
    </div>
    
    <div class="knowledge-bento">

        <div class="bento-grid">
            <!-- Temas - Bloque grande principal -->
            <div class="bento-item large temas" onclick="showTemas()">
                <div class="bento-content">
                    <div class="bento-header">
                        <span class="bento-icon">📚</span>
                        <span class="bento-number">6</span>
                    </div>
                    <h4 class="bento-title">Temas</h4>
                    <p class="bento-desc">Explora tu contenido</p>
                    <div class="bento-decoration">
                        <div class="floating-dots">
                            <span>🔐</span>
                            <span>🛡️</span>
                            <span>🔑</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Certificaciones - Bloque vertical -->
            <div class="bento-item tall certs" onclick="showCertificaciones()">
                <div class="bento-content">
                    <span class="bento-icon cert-icon">🎓</span>
                    <span class="bento-number">3</span>
                    <h4 class="bento-title">Certificaciones</h4>
                </div>
            </div>

            <!-- Ejercicios - Bloque interactivo -->
            <div class="bento-item medium exercises" onclick="goToLaboratorio()">
                <div class="bento-content exercises-layout">
                    <div class="exercises-left">
                        <div class="lab-animation">
                            <span class="bento-icon lab-icon">🧪</span>
                            <div class="bubbles">
                                <span></span>
                                <span></span>
                                <span></span>
                            </div>
                        </div>
                    </div>
                    <div class="exercises-right">
                        <span class="bento-number">5</span>
                        <h4 class="bento-title">Ejercicios</h4>
                        <p class="bento-desc">Práctica hands-on</p>
                    </div>
                </div>
            </div>

            <!-- Tema Aleatorio - Bloque especial -->
            <div class="bento-item wide random-dice" onclick="console.log('🎲 Onclick directo funcionando'); showRandomTopic()">
                <div class="bento-content">
                    <div class="dice-container">
                        <span class="bento-icon dice-icon">🎲</span>
                        <div class="dice-glow"></div>
                    </div>
                    <div class="random-text">
                        <h4 class="bento-title">Tema Aleatorio</h4>
                        <p class="bento-desc">¿Qué estudiamos hoy?</p>
                    
                    </div>
                    <div class="sparkles">
                        <span>✨</span>
                        <span>⭐</span>
                        <span>✨</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Asegurar que el tema aleatorio funcione
        const randomDice = document.querySelector('.random-dice');
        if (randomDice) {
            randomDice.addEventListener('click', function(e) {
                console.log('🎲 Click detectado en random-dice');
                e.preventDefault();
                showRandomTopic();
            });
            console.log('✅ Event listener agregado a random-dice');
        } else {
            console.log('❌ No se encontró .random-dice');
        }

        // Hover effects para tech icons
        const techItems = document.querySelectorAll('.tech-item');
        
        techItems.forEach(item => {
            item.addEventListener('mouseenter', function() {
                this.style.transform = 'scale(1.1) translateY(-5px)';
            });
            
            item.addEventListener('mouseleave', function() {
                this.style.transform = 'scale(1) translateY(0)';
            });
        });

        // Animación de entrada para stats
        const statItems = document.querySelectorAll('.stat-item');
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry, index) => {
                if (entry.isIntersecting) {
                    setTimeout(() => {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0) scale(1)';
                    }, index * 100);
                }
            });
        }, { threshold: 0.1 });

        statItems.forEach(item => {
            item.style.opacity = '0';
            item.style.transform = 'translateY(20px) scale(0.95)';
            item.style.transition = 'all 0.6s ease';
            observer.observe(item);
        });
    });

    // Funciones para las stats clickeables
    function showTemas() {
        // Scroll al hub de categorías
        const categoriesTab = document.getElementById('categories-tab');
        if (categoriesTab) {
            categoriesTab.scrollIntoView({ 
                behavior: 'smooth' 
            });
        }
    }

    function showCertificaciones() {
        // Cambiar al tab de certificaciones
        const tabBtn = document.querySelector('[data-tab="certifications"]');
        const tabPanel = document.getElementById('certifications-tab');
        
        if (tabBtn && tabPanel) {
            // Activar tab
            document.querySelectorAll('.tab-btn').forEach(btn => btn.classList.remove('active'));
            document.querySelectorAll('.tab-panel').forEach(panel => panel.classList.remove('active'));
            
            tabBtn.classList.add('active');
            tabPanel.classList.add('active');
            
            // Scroll suave
            tabPanel.scrollIntoView({ behavior: 'smooth' });
        }
    }

    function goToLaboratorio() {
        window.location.href = '/cyber/interactivo';
    }

    function showRandomTopic() {
        console.log('🎲 Iniciando tema aleatorio simplificado...');
        
        // Lista fija de temas disponibles (sin fetch para evitar problemas)
        const availableTopics = [
            { title: 'Criptografía', url: '/cyber/criptografia', icon: '🔐', category: 'Ciberseguridad', status: 'completed', description: 'Fundamentos de criptografía y algoritmos de cifrado' },
            { title: 'CIA Triad', url: '/cyber/CIA', icon: '🔺', category: 'Ciberseguridad', status: 'completed', description: 'Los tres pilares fundamentales de la seguridad informática' },
            { title: 'PKI', url: '/cyber/pki', icon: '🔑', category: 'Ciberseguridad', status: 'completed', description: 'Infraestructura de Clave Pública y certificados digitales' },
            { title: 'NIST Framework', url: '/cyber/nist', icon: '📋', category: 'Ciberseguridad', status: 'completed', description: 'Marco de trabajo y estándares de seguridad del NIST' },
            { title: 'Laboratorio Interactivo', url: '/cyber/interactivo', icon: '🎮', category: 'Ciberseguridad', status: 'interactive', description: 'Herramientas interactivas para practicar criptografía' }
        ];
        
        const randomTopic = availableTopics[Math.floor(Math.random() * availableTopics.length)];
        console.log('🎯 Tema seleccionado:', randomTopic);
        
        // Crear modal simple
        const modal = document.createElement('div');
        modal.style.cssText = `
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: rgba(0,0,0,0.8);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 999999;
            backdrop-filter: blur(10px);
        `;
        
        modal.innerHTML = `
            <div style="
                background: linear-gradient(135deg, #1e293b, #334155);
                border-radius: 1rem;
                max-width: 400px;
                width: 90%;
                border: 1px solid rgba(148, 163, 184, 0.2);
                overflow: hidden;
                box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.8);
            ">
                <div style="
                    padding: 1.5rem;
                    border-bottom: 1px solid rgba(148, 163, 184, 0.1);
                    display: flex;
                    justify-content: space-between;
                    align-items: center;
                ">
                    <h3 style="color: #f8fafc; margin: 0; font-size: 1.25rem;">🎲 Tema Aleatorio</h3>
                    <button onclick="closeRandomModal()" style="
                        background: none;
                        border: none;
                        color: #94a3b8;
                        font-size: 1.5rem;
                        cursor: pointer;
                        padding: 0.25rem;
                        border-radius: 0.25rem;
                        transition: all 0.3s ease;
                    ">×</button>
                </div>
                <div style="padding: 1.5rem; text-align: center;">
                    <span style="font-size: 3rem; display: block; margin-bottom: 1rem;">${randomTopic.icon}</span>
                    <h4 style="color: #f8fafc; margin-bottom: 0.5rem; font-size: 1.25rem;">${randomTopic.title}</h4>
                    <p style="color: #cbd5e1; margin-bottom: 1rem; font-size: 0.9rem; line-height: 1.4;">${randomTopic.description}</p>
                    <div style="display: flex; gap: 0.5rem; justify-content: center; margin-bottom: 1.5rem; flex-wrap: wrap;">
                        <span style="
                            padding: 0.25rem 0.75rem;
                            border-radius: 1rem;
                            font-size: 0.75rem;
                            font-weight: 600;
                            text-transform: uppercase;
                            background: rgba(59, 130, 246, 0.2);
                            color: #93c5fd;
                            border: 1px solid rgba(59, 130, 246, 0.3);
                        ">${randomTopic.category}</span>
                        <span style="
                            padding: 0.25rem 0.75rem;
                            border-radius: 1rem;
                            font-size: 0.75rem;
                            font-weight: 600;
                            text-transform: uppercase;
                            background: rgba(34, 197, 94, 0.2);
                            color: #86efac;
                            border: 1px solid rgba(34, 197, 94, 0.3);
                        ">${randomTopic.status}</span>
                    </div>
                    <div style="display: flex; gap: 0.75rem; justify-content: center;">
                        <button onclick="window.location.href='${randomTopic.url}'" style="
                            padding: 0.75rem 1.25rem;
                            border: none;
                            border-radius: 0.5rem;
                            font-weight: 500;
                            cursor: pointer;
                            transition: all 0.3s ease;
                            font-size: 0.875rem;
                            background: linear-gradient(45deg, #3b82f6, #8b5cf6);
                            color: white;
                        ">📖 Ir al tema</button>
                        <button onclick="closeRandomModal(); showRandomTopic()" style="
                            padding: 0.75rem 1.25rem;
                            border: 1px solid rgba(148, 163, 184, 0.2);
                            border-radius: 0.5rem;
                            font-weight: 500;
                            cursor: pointer;
                            transition: all 0.3s ease;
                            font-size: 0.875rem;
                            background: rgba(255, 255, 255, 0.1);
                            color: #cbd5e1;
                        ">🔄 Otro tema</button>
                    </div>
                </div>
            </div>
        `;
        
        // Agregar al DOM
        modal.id = 'randomTopicModal';
        document.body.appendChild(modal);
        
        // Cerrar al hacer click fuera
        modal.addEventListener('click', function(e) {
            if (e.target === modal) {
                closeRandomModal();
            }
        });
        
        console.log('📱 Modal creado correctamente');
    }
    
    function closeRandomModal() {
        const modal = document.getElementById('randomTopicModal');
        if (modal) {
            modal.remove();
            console.log('📱 Modal cerrado');
        }
    }
</script>

<style>
    /* Tech Stats Section */
    .tech-stats-section {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 30px;
        margin-bottom: 40px;
    }

    .tech-grid h3, .knowledge-stats h3 {
        color: white;
        font-size: 1.5em;
        margin-bottom: 20px;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
    }

    .tech-icons {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(80px, 1fr));
        gap: 15px;
    }

    .tech-item {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
        cursor: pointer;
    }

    .tech-icon {
        width: 50px;
        height: 50px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5em;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        transition: all 0.3s ease;
    }

    /* Tech Icon Colors */
    .tech-icon.python { 
        background: linear-gradient(45deg, #3776ab, #ffd343); 
    }
    
    .tech-icon.javascript { 
        background: linear-gradient(45deg, #f7df1e, #000); 
        color: black; 
    }
    
    .tech-icon.docker { 
        background: linear-gradient(45deg, #2496ed, #0db7ed); 
    }
    
    .tech-icon.linux { 
        background: linear-gradient(45deg, #fcc624, #000); 
    }
    
    .tech-icon.aws { 
        background: linear-gradient(45deg, #ff9900, #232f3e); 
    }
    
    .tech-icon.k8s { 
        background: linear-gradient(45deg, #326ce5, #fff); 
    }
    
    .tech-icon.git { 
        background: linear-gradient(45deg, #f05032, #fff); 
    }
    
    .tech-icon.sql { 
        background: linear-gradient(45deg, #336791, #fff); 
    }
    
    .tech-icon.astro { 
        background: linear-gradient(45deg, #ff5d01, #000); 
    }
    
    .tech-icon.tailwind { 
        background: linear-gradient(45deg, #06b6d4, #0891b2); 
    }

    .tech-name {
        font-size: 0.75em;
        color: rgba(255, 255, 255, 0.9);
        font-weight: 500;
        text-align: center;
    }

    /* Bento Box Knowledge Stats */
    .knowledge-bento .bento-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        grid-template-rows: auto auto;
        gap: 20px;
        grid-template-areas: 
            "temas certs"
            "exercises random";
        height: 320px;
    }

    .bento-item {
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(20px);
        border-radius: 24px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        cursor: pointer;
        overflow: hidden;
        position: relative;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    }

    .bento-item::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(135deg, rgba(255,255,255,0.1), rgba(255,255,255,0.05));
        opacity: 0;
        transition: opacity 0.4s ease;
        border-radius: inherit;
    }

    .bento-item:hover::before {
        opacity: 1;
    }

    .bento-item:hover {
        transform: translateY(-8px) rotate(0.5deg);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
        border-color: rgba(255, 255, 255, 0.2);
    }

    /* Layouts específicos */
    .bento-item.large {
        grid-area: temas;
    }

    .bento-item.tall {
        grid-area: certs;
    }

    .bento-item.medium {
        grid-area: exercises;
    }

    .bento-item.wide {
        grid-area: random;
    }

    .bento-content {
        padding: 24px;
        height: 100%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        position: relative;
        z-index: 2;
    }

    /* Temas - Bloque principal */
    .bento-item.temas {
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
        border-color: rgba(59, 130, 246, 0.2);
    }

    .bento-item.temas:hover {
        box-shadow: 0 20px 40px rgba(59, 130, 246, 0.3);
        transform: translateY(-8px) rotate(-0.5deg);
    }

    .bento-header {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 12px;
        margin-bottom: 16px;
        width: 100%;
    }

    .floating-dots {
        display: flex;
        justify-content: center;
        gap: 8px;
        margin-top: auto;
        width: 100%;
    }

    .floating-dots span {
        font-size: 1.2em;
        opacity: 0.6;
        animation: float 3s ease-in-out infinite;
    }

    .floating-dots span:nth-child(2) {
        animation-delay: 0.5s;
    }

    .floating-dots span:nth-child(3) {
        animation-delay: 1s;
    }

    @keyframes float {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        50% { transform: translateY(-8px) rotate(5deg); }
    }

    /* Certificaciones - Bloque vertical */
    .bento-item.certs {
        background: linear-gradient(135deg, rgba(245, 158, 11, 0.1), rgba(251, 191, 36, 0.1));
        border-color: rgba(245, 158, 11, 0.2);
    }

    .bento-item.certs:hover {
        box-shadow: 0 20px 40px rgba(245, 158, 11, 0.3);
    }

    .cert-icon {
        display: block;
        text-align: center;
        margin-bottom: 12px;
    }

    /* Ejercicios - Bloque con animación */
    .bento-item.exercises {
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(6, 182, 212, 0.1));
        border-color: rgba(16, 185, 129, 0.2);
    }

    .bento-item.exercises:hover {
        box-shadow: 0 20px 40px rgba(16, 185, 129, 0.3);
    }

    /* Layout específico para ejercicios */
    .bento-content.exercises-layout {
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
        gap: 16px;
        text-align: left;
    }

    .exercises-left {
        flex: 0 0 auto;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .exercises-right {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
    }

    .lab-animation {
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .lab-icon {
        position: relative;
        z-index: 2;
    }

    .bubbles {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
    }

    .bubbles span {
        position: absolute;
        width: 6px;
        height: 6px;
        background: rgba(16, 185, 129, 0.6);
        border-radius: 50%;
        animation: bubble 2s infinite;
    }

    .bubbles span:nth-child(1) {
        left: -20px;
        animation-delay: 0s;
    }

    .bubbles span:nth-child(2) {
        left: 0px;
        animation-delay: 0.7s;
    }

    .bubbles span:nth-child(3) {
        left: 20px;
        animation-delay: 1.4s;
    }

    @keyframes bubble {
        0% { transform: translateY(0); opacity: 1; }
        50% { transform: translateY(-15px); opacity: 0.7; }
        100% { transform: translateY(-30px); opacity: 0; }
    }

    /* Tema Aleatorio - Bloque especial */
    .bento-item.random-dice {
        background: linear-gradient(135deg, rgba(147, 51, 234, 0.1), rgba(79, 70, 229, 0.1));
        border-color: rgba(147, 51, 234, 0.2);
    }

    .bento-item.random-dice:hover {
        box-shadow: 0 20px 40px rgba(147, 51, 234, 0.3);
        transform: translateY(-8px) rotate(1deg);
    }

    .bento-item.random-dice .bento-content {
        flex-direction: row;
        align-items: center;
        justify-content: center;
        gap: 16px;
        text-align: left;
    }

    .dice-container {
        position: relative;
        flex-shrink: 0;
    }

    .dice-icon {
        font-size: 2em;
        animation: spin 4s linear infinite;
        position: relative;
        z-index: 2;
    }

    .dice-glow {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 40px;
        height: 40px;
        background: radial-gradient(circle, rgba(147, 51, 234, 0.3), transparent);
        border-radius: 50%;
        animation: pulse 2s ease-in-out infinite;
    }

    @keyframes spin {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
    }

    @keyframes pulse {
        0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.7; }
        50% { transform: translate(-50%, -50%) scale(1.2); opacity: 0.3; }
    }

    .random-text {
        flex: 1;
    }

    .sparkles {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 4px;
        opacity: 0.7;
        flex-shrink: 0;
    }

    .sparkles span {
        animation: twinkle 1.5s ease-in-out infinite;
    }

    .sparkles span:nth-child(2) {
        animation-delay: 0.5s;
    }

    .sparkles span:nth-child(3) {
        animation-delay: 1s;
    }

    @keyframes twinkle {
        0%, 100% { opacity: 0.7; transform: scale(1); }
        50% { opacity: 1; transform: scale(1.2); }
    }

    /* Estilos de texto comunes */
    .bento-icon {
        font-size: 2em;
        filter: drop-shadow(0 0 12px rgba(255, 255, 255, 0.3));
    }

    .bento-number {
        font-size: 2.5em;
        font-weight: 800;
        color: white;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        line-height: 1;
    }

    .bento-title {
        color: white;
        font-size: 1.1em;
        font-weight: 600;
        margin: 8px 0 4px 0;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
        text-align: center;
        width: 100%;
    }

    .bento-desc {
        color: rgba(255, 255, 255, 0.7);
        font-size: 0.85em;
        margin: 0;
        line-height: 1.4;
        text-align: center;
        width: 100%;
    }

    /* Modal para tema aleatorio */
    .random-topic-modal {
        position: fixed !important;
        top: 0 !important;
        left: 0 !important;
        width: 100vw !important;
        height: 100vh !important;
        background: rgba(0, 0, 0, 0.8) !important;
        display: flex !important;
        align-items: center !important;
        justify-content: center !important;
        z-index: 999999 !important;
        backdrop-filter: blur(10px) !important;
        margin: 0 !important;
        padding: 0 !important;
    }

    .modal-content {
        background: linear-gradient(135deg, #1e293b, #334155) !important;
        border-radius: 1rem !important;
        max-width: 400px !important;
        width: 90% !important;
        max-height: 90vh !important;
        border: 1px solid rgba(148, 163, 184, 0.2) !important;
        overflow: hidden !important;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.8) !important;
        position: relative !important;
        margin: auto !important;
    }

    .modal-header {
        padding: 1.5rem;
        border-bottom: 1px solid rgba(148, 163, 184, 0.1);
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .modal-header h3 {
        color: #f8fafc;
        margin: 0;
        font-size: 1.25rem;
    }

    .close-btn {
        background: none;
        border: none;
        color: #94a3b8;
        font-size: 1.5rem;
        cursor: pointer;
        padding: 0.25rem;
        border-radius: 0.25rem;
        transition: all 0.3s ease;
    }

    .close-btn:hover {
        background: rgba(255, 255, 255, 0.1);
        color: #f8fafc;
    }

    .modal-body {
        padding: 1.5rem;
    }

    .random-topic-card {
        text-align: center;
    }

    .topic-icon {
        font-size: 3rem;
        display: block;
        margin-bottom: 1rem;
    }

    .random-topic-card h4 {
        color: #f8fafc;
        margin-bottom: 0.5rem;
        font-size: 1.25rem;
    }

    .random-topic-card p {
        color: #cbd5e1;
        margin-bottom: 1.5rem;
    }

    .topic-actions {
        display: flex;
        gap: 0.75rem;
        justify-content: center;
    }

    .go-btn, .shuffle-btn {
        padding: 0.75rem 1.25rem;
        border: none;
        border-radius: 0.5rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.3s ease;
        font-size: 0.875rem;
    }

    .go-btn {
        background: linear-gradient(45deg, #3b82f6, #8b5cf6);
        color: white;
    }

    .go-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
    }

    .shuffle-btn {
        background: rgba(255, 255, 255, 0.1);
        color: #cbd5e1;
        border: 1px solid rgba(148, 163, 184, 0.2);
    }

    .shuffle-btn:hover {
        background: rgba(255, 255, 255, 0.2);
        color: #f8fafc;
    }

    /* Nuevos estilos para descripción y badges */
    .topic-description {
        color: #cbd5e1;
        margin-bottom: 1rem;
        font-size: 0.9rem;
        line-height: 1.4;
    }

    .topic-category {
        display: flex;
        gap: 0.5rem;
        justify-content: center;
        margin-bottom: 1.5rem;
        flex-wrap: wrap;
    }

    .category-badge, .status-badge {
        padding: 0.25rem 0.75rem;
        border-radius: 1rem;
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .category-badge {
        background: rgba(59, 130, 246, 0.2);
        color: #93c5fd;
        border: 1px solid rgba(59, 130, 246, 0.3);
    }

    .status-badge {
        border: 1px solid transparent;
    }

    .status-completed {
        background: rgba(34, 197, 94, 0.2);
        color: #86efac;
        border-color: rgba(34, 197, 94, 0.3);
    }

    .status-interactive {
        background: rgba(168, 85, 247, 0.2);
        color: #c4b5fd;
        border-color: rgba(168, 85, 247, 0.3);
    }

    .status-planned {
        background: rgba(251, 191, 36, 0.2);
        color: #fcd34d;
        border-color: rgba(251, 191, 36, 0.3);
    }

    /* Responsive */
    @media (max-width: 768px) {
        .tech-stats-section {
            grid-template-columns: 1fr;
            gap: 20px;
        }
        
        .tech-icons {
            grid-template-columns: repeat(auto-fit, minmax(60px, 1fr));
            gap: 10px;
        }
        
        .tech-icon {
            width: 40px;
            height: 40px;
            font-size: 1.2em;
        }
        
        .tech-name {
            font-size: 0.7em;
        }
        
        .knowledge-bento .bento-grid {
            grid-template-columns: 1fr;
            grid-template-rows: auto auto auto auto;
            grid-template-areas: 
                "temas"
                "certs"
                "exercises"
                "random";
            height: auto;
            gap: 16px;
        }

        .bento-content {
            padding: 20px;
        }

        .bento-header {
            flex-direction: column;
            gap: 8px;
        }

        .bento-content.exercises-layout {
            flex-direction: column;
            gap: 12px;
        }

        .exercises-left, .exercises-right {
            width: 100%;
            text-align: center;
        }

        .bento-item.random-dice .bento-content {
            flex-direction: column;
            text-align: center;
            gap: 12px;
            align-items: center;
            justify-content: center;
        }

        .random-text {
            text-align: center;
            width: 100%;
        }

        .bento-number {
            font-size: 2em;
        }
    }

    @media (max-width: 480px) {
        .tech-icons {
            grid-template-columns: repeat(5, 1fr);
        }
        
        .knowledge-bento .bento-grid {
            gap: 12px;
        }

        .bento-content {
            padding: 16px;
        }

        .bento-item.random-dice .bento-content {
            padding: 12px;
        }

        .bento-title {
            font-size: 1em;
        }

        .bento-number {
            font-size: 1.8em;
        }

        .bento-icon {
            font-size: 1.5em;
        }
        
        .tech-grid h3, .knowledge-bento h3 {
            font-size: 1.3em;
        }

        .topic-actions {
            flex-direction: column;
        }
    }
</style>