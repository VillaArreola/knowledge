---
import WikiLayout from '../../layouts/WikiLayout.astro';
---

<WikiLayout title="PKI - Infraestructura de Clave Pública" description="Guía completa sobre PKI: componentes, certificados y proceso de implementación">
    <!-- Hero Section -->
    <div class="hero-section relative overflow-hidden">
        <div class="hero-bg"></div>
        <div class="container mx-auto px-4 py-16 relative z-10">
            <div class="text-center mb-12">
                <div class="security-icon">🔐</div>
                <h1 class="hero-title">Infraestructura de Clave Pública</h1>
                <p class="hero-subtitle">El ecosistema que protege nuestra comunicación digital</p>
            </div>
        </div>
    </div>

    <!-- Content Sections -->
    <div class="container mx-auto px-4 py-8">
        <!-- What is PKI -->
        <div class="content-card animate-slide-up">
            <div class="card-header">
                <h2>¿Qué es PKI?</h2>
            </div>
            <p class="text-lg leading-relaxed mb-6">
                La <strong>PKI (Public Key Infrastructure)</strong> es un ecosistema compuesto por 
                <span class="highlight">políticas, procedimientos, software y hardware</span> 
                necesarios para <strong>crear, distribuir, usar y revocar certificados digitales</strong>.
            </p>
            
            <div class="security-features">
                <div class="feature-item">
                    <div class="feature-icon">🔒</div>
                    <span>Autenticidad</span>
                </div>
                <div class="feature-item">
                    <div class="feature-icon">🛡️</div>
                    <span>Confidencialidad</span>
                </div>
                <div class="feature-item">
                    <div class="feature-icon">✅</div>
                    <span>Integridad</span>
                </div>
                <div class="feature-item">
                    <div class="feature-icon">📝</div>
                    <span>No repudio</span>
                </div>
            </div>
        </div>

        <!-- Components -->
        <div class="content-card animate-slide-up">
            <div class="card-header">
                <h2>🧩 Componentes de la PKI</h2>
            </div>
            
            <div class="components-grid">
                <div class="component-card ca-card">
                    <h3>CA</h3>
                    <p>Autoridad Certificadora</p>
                    <div class="component-desc">Emite y firma certificados digitales</div>
                </div>
                
                <div class="component-card ra-card">
                    <h3>RA</h3>
                    <p>Autoridad de Registro</p>
                    <div class="component-desc">Verifica la identidad del solicitante</div>
                </div>
                
                <div class="component-card directory-card">
                    <h3>Servidor</h3>
                    <p>de Directorio</p>
                    <div class="component-desc">Almacena y distribuye certificados</div>
                </div>
                
                <div class="component-card entity-card">
                    <h3>Entidad</h3>
                    <p>Final</p>
                    <div class="component-desc">Persona, dispositivo o aplicación que usa el certificado</div>
                </div>
            </div>
        </div>

        <!-- Certificate Types -->
        <div class="content-card animate-slide-up">
            <div class="card-header">
                <h2>📜 Tipos de Certificados</h2>
            </div>
            
            <div class="certificates-comparison">
                <div class="cert-type with-keys">
                    <div class="cert-header">
                        <h3>Con Claves Públicas</h3>
                        <div class="cert-status has-keys">✅</div>
                    </div>
                    <p>Cifrado, autenticación y firma digital</p>
                </div>
                
                <div class="cert-type without-keys">
                    <div class="cert-header">
                        <h3>Sin Claves Públicas</h3>
                        <div class="cert-status no-keys">❌</div>
                    </div>
                    <p>Define políticas, incluye listas de revocación (CRL)</p>
                </div>
            </div>
            
            <div class="standard-note">
                <h4>📎 Estándar: X.509 versión 3</h4>
                <div class="x509-fields">
                    <span>Número de serie</span>
                    <span>Algoritmo de firma</span>
                    <span>Periodo de validez</span>
                    <span>Nombre del emisor</span>
                    <span>Clave pública</span>
                    <span>Uso de clave</span>
                </div>
            </div>
        </div>

        <!-- Process Flow -->
        <div class="content-card animate-slide-up">
            <div class="card-header">
                <h2>🔄 Proceso de Solicitud</h2>
            </div>
            
            <div class="process-flow">
                <div class="process-step">
                    <div class="step-number">1</div>
                    <div class="step-content">
                        <h3>Solicitud CSR</h3>
                        <p>El usuario hace una solicitud de certificado a través de la RA</p>
                    </div>
                </div>
                
                <div class="process-arrow">→</div>
                
                <div class="process-step">
                    <div class="step-number">2</div>
                    <div class="step-content">
                        <h3>Verificación</h3>
                        <p>La RA verifica la identidad y aprueba la solicitud</p>
                    </div>
                </div>
                
                <div class="process-arrow">→</div>
                
                <div class="process-step">
                    <div class="step-number">3</div>
                    <div class="step-content">
                        <h3>Emisión</h3>
                        <p>La CA emite y firma el certificado digital</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Quiz Section -->
        <div class="content-card quiz-section animate-slide-up">
            <div class="card-header">
                <h2>🧠 Quiz de Conocimientos</h2>
            </div>
            
            <div id="quiz-container">
                <div class="quiz-question active">
                    <h3>¿Cuál es la función principal de la CA?</h3>
                    <div class="quiz-options">
                        <button class="quiz-option" data-correct="false">Verificar la identidad del solicitante</button>
                        <button class="quiz-option" data-correct="true">Emitir y firmar certificados digitales</button>
                        <button class="quiz-option" data-correct="false">Almacenar certificados</button>
                        <button class="quiz-option" data-correct="false">Revocar certificados únicamente</button>
                    </div>
                </div>
                
                <div class="quiz-question">
                    <h3>¿Qué estándar siguen los certificados digitales?</h3>
                    <div class="quiz-options">
                        <button class="quiz-option" data-correct="false">X.508 v2</button>
                        <button class="quiz-option" data-correct="true">X.509 v3</button>
                        <button class="quiz-option" data-correct="false">SSL/TLS</button>
                        <button class="quiz-option" data-correct="false">RSA-2048</button>
                    </div>
                </div>
                
                <div class="quiz-question">
                    <h3>¿Cuál NO es una garantía de la PKI?</h3>
                    <div class="quiz-options">
                        <button class="quiz-option" data-correct="false">Autenticidad</button>
                        <button class="quiz-option" data-correct="false">Confidencialidad</button>
                        <button class="quiz-option" data-correct="true">Velocidad de transmisión</button>
                        <button class="quiz-option" data-correct="false">No repudio</button>
                    </div>
                </div>
            </div>
            
            <div class="quiz-controls">
                <button id="prevBtn" class="quiz-btn secondary" disabled>Anterior</button>
                <div class="quiz-progress">
                    <span id="currentQ">1</span> / <span id="totalQ">3</span>
                </div>
                <button id="nextBtn" class="quiz-btn primary">Siguiente</button>
            </div>
            
            <div id="quiz-results" class="quiz-results hidden">
                <div class="results-content">
                    <h3>¡Quiz Completado!</h3>
                    <div class="score-display">
                        <span id="score">0</span> / <span id="total">3</span>
                    </div>
                    <p id="feedback"></p>
                    <button id="restartBtn" class="quiz-btn primary">Reiniciar Quiz</button>
                </div>
            </div>
        </div>

        <!-- Hierarchy Section -->
        <div class="content-card animate-slide-up">
            <div class="card-header">
                <h2>🏛️ Jerarquía de CA</h2>
            </div>
            
            <div class="hierarchy-diagram">
                <div class="ca-level root-ca">
                    <div class="ca-node">
                        <span>CA Raíz</span>
                    </div>
                </div>
                
                <div class="hierarchy-lines">
                    <div class="line"></div>
                    <div class="line"></div>
                </div>
                
                <div class="ca-level subordinate-cas">
                    <div class="ca-node">
                        <span>CA Subordinada A</span>
                    </div>
                    <div class="ca-node">
                        <span>CA Subordinada B</span>
                    </div>
                </div>
            </div>
            
            <div class="cross-certification">
                <p><strong>Certificación Cruzada:</strong> Las PKI pueden confiar entre sí mediante certificación cruzada, donde una CA de una PKI certifica a otra CA.</p>
            </div>
        </div>
    </div>
</WikiLayout>

<script>
    // Quiz functionality
    let currentQuestion = 0;
    let score = 0;
    let answered = false;
    const totalQuestions = 3;

    const questions = document.querySelectorAll('.quiz-question');
    const prevBtn = document.getElementById('prevBtn');
    const nextBtn = document.getElementById('nextBtn');
    const currentQSpan = document.getElementById('currentQ');
    const resultsDiv = document.getElementById('quiz-results');

    function showQuestion(index) {
        questions.forEach((q, i) => {
            q.classList.toggle('active', i === index);
        });
        
        currentQSpan.textContent = index + 1;
        prevBtn.disabled = index === 0;
        
        if (index === totalQuestions - 1) {
            nextBtn.textContent = 'Finalizar';
        } else {
            nextBtn.textContent = 'Siguiente';
        }
        
        answered = false;
        nextBtn.disabled = true;
    }

    function handleAnswerClick(e) {
        if (answered) return;
        
        const options = e.target.closest('.quiz-question').querySelectorAll('.quiz-option');
        const isCorrect = e.target.dataset.correct === 'true';
        
        options.forEach(opt => {
            opt.disabled = true;
            if (opt.dataset.correct === 'true') {
                opt.classList.add('correct');
            } else if (opt === e.target && !isCorrect) {
                opt.classList.add('incorrect');
            }
        });
        
        if (isCorrect) score++;
        answered = true;
        nextBtn.disabled = false;
    }

    function showResults() {
        resultsDiv.classList.remove('hidden');
        document.getElementById('score').textContent = score;
        document.getElementById('total').textContent = totalQuestions;
        
        const percentage = (score / totalQuestions) * 100;
        const feedback = document.getElementById('feedback');
        
        if (percentage >= 80) {
            feedback.textContent = '¡Excelente! Dominas los conceptos de PKI.';
            feedback.className = 'excellent';
        } else if (percentage >= 60) {
            feedback.textContent = 'Bien, pero puedes mejorar revisando algunos conceptos.';
            feedback.className = 'good';
        } else {
            feedback.textContent = 'Te recomendamos revisar el contenido nuevamente.';
            feedback.className = 'needs-improvement';
        }
    }

    function restartQuiz() {
        currentQuestion = 0;
        score = 0;
        answered = false;
        
        questions.forEach(q => {
            const options = q.querySelectorAll('.quiz-option');
            options.forEach(opt => {
                opt.disabled = false;
                opt.classList.remove('correct', 'incorrect');
            });
        });
        
        resultsDiv.classList.add('hidden');
        showQuestion(0);
    }

    // Event listeners
    document.addEventListener('click', (e) => {
        if (e.target.classList.contains('quiz-option')) {
            handleAnswerClick(e);
        }
    });

    nextBtn.addEventListener('click', () => {
        if (currentQuestion < totalQuestions - 1) {
            currentQuestion++;
            showQuestion(currentQuestion);
        } else {
            showResults();
        }
    });

    prevBtn.addEventListener('click', () => {
        if (currentQuestion > 0) {
            currentQuestion--;
            showQuestion(currentQuestion);
        }
    });

    document.getElementById('restartBtn').addEventListener('click', restartQuiz);

    // Scroll animations
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('visible');
            }
        });
    }, observerOptions);

    document.querySelectorAll('.animate-slide-up').forEach(el => {
        observer.observe(el);
    });

    // Initialize
    showQuestion(0);
</script>

<style>
    /* Hero Section */
    .hero-section {
        min-height: 60vh;
        display: flex;
        align-items: center;
        position: relative;
    }

    .hero-bg {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        opacity: 0.9;
    }

    .hero-bg::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse"><path d="M 10 0 L 0 0 0 10" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="1"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
    }

    .security-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        animation: pulse-security 2s ease-in-out infinite;
    }

    @keyframes pulse-security {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.1); }
    }

    .hero-title {
        font-size: 3.5rem;
        font-weight: 700;
        margin-bottom: 1rem;
        background: linear-gradient(135deg, #fff 0%, #f0f0f0 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
    }

    .hero-subtitle {
        font-size: 1.25rem;
        color: rgba(255, 255, 255, 0.9);
        font-weight: 300;
    }

    /* Content Cards */
    .content-card {
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0.05) 100%);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 20px;
        padding: 2rem;
        margin-bottom: 2rem;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
        opacity: 0;
        transform: translateY(30px);
    }

    .content-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
    }

    .content-card.visible {
        opacity: 1;
        transform: translateY(0);
    }

    .card-header h2 {
        font-size: 2rem;
        font-weight: 600;
        margin-bottom: 1.5rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .highlight {
        background: linear-gradient(135deg, #ffd89b 0%, #19547b 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 600;
    }

    /* Security Features */
    .security-features {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
        margin-top: 2rem;
    }

    .feature-item {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 15px;
        transition: all 0.3s ease;
    }

    .feature-item:hover {
        background: rgba(255, 255, 255, 0.1);
        transform: translateY(-3px);
    }

    .feature-icon {
        font-size: 2rem;
        filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.3));
    }

    /* Components Grid */
    .components-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.5rem;
        margin-top: 2rem;
    }

    .component-card {
        padding: 2rem;
        border-radius: 15px;
        text-align: center;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }

    .component-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        opacity: 0.1;
        z-index: 0;
    }

    .component-card > * {
        position: relative;
        z-index: 1;
    }

    .ca-card {
        background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
    }

    .ra-card {
        background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
    }

    .directory-card {
        background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
    }

    .entity-card {
        background: linear-gradient(135deg, #d299c2 0%, #fef9d7 100%);
    }

    .component-card:hover {
        transform: translateY(-10px) scale(1.02);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    }

    .component-card h3 {
        font-size: 1.8rem;
        font-weight: 700;
        color: #1a1a1a;
        margin-bottom: 0.5rem;
    }

    .component-card p {
        font-weight: 600;
        color: #2a2a2a;
        margin-bottom: 1rem;
    }

    .component-desc {
        color: #4a4a4a;
        font-size: 0.9rem;
    }

    /* Certificate Types */
    .certificates-comparison {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin: 2rem 0;
    }

    .cert-type {
        padding: 2rem;
        border-radius: 15px;
        transition: all 0.3s ease;
    }

    .with-keys {
        background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    }

    .without-keys {
        background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
    }

    .cert-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
    }

    .cert-header h3 {
        color: #1a1a1a;
        font-size: 1.5rem;
        font-weight: 600;
    }

    .cert-status {
        font-size: 1.5rem;
    }

    .cert-type p {
        color: #2a2a2a;
        font-weight: 500;
    }

    .standard-note {
        margin-top: 2rem;
        padding: 1.5rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 15px;
        border-left: 4px solid #667eea;
    }

    .x509-fields {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
        margin-top: 1rem;
    }

    .x509-fields span {
        padding: 0.5rem 1rem;
        background: rgba(102, 126, 234, 0.2);
        border-radius: 20px;
        font-size: 0.9rem;
        border: 1px solid rgba(102, 126, 234, 0.3);
    }

    /* Process Flow */
    .process-flow {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-wrap: wrap;
        gap: 2rem;
        margin: 2rem 0;
    }

    .process-step {
        display: flex;
        flex-direction: column;
        align-items: center;
        max-width: 200px;
        text-align: center;
    }

    .step-number {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        font-weight: 700;
        margin-bottom: 1rem;
        box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
    }

    .step-content h3 {
        font-size: 1.2rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: #667eea;
    }

    .process-arrow {
        font-size: 2rem;
        color: #667eea;
        font-weight: bold;
    }

    /* Quiz Styles */
    .quiz-section {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
    }

    .quiz-question {
        display: none;
        margin-bottom: 2rem;
    }

    .quiz-question.active {
        display: block;
        animation: fadeInSlide 0.5s ease;
    }

    @keyframes fadeInSlide {
        from {
            opacity: 0;
            transform: translateX(20px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    .quiz-question h3 {
        font-size: 1.3rem;
        margin-bottom: 1.5rem;
        color: #667eea;
    }

    .quiz-options {
        display: grid;
        gap: 1rem;
    }

    .quiz-option {
        padding: 1rem 1.5rem;
        background: rgba(255, 255, 255, 0.05);
        border: 2px solid rgba(255, 255, 255, 0.1);
        border-radius: 10px;
        color: white;
        cursor: pointer;
        transition: all 0.3s ease;
        text-align: left;
    }

    .quiz-option:hover:not(:disabled) {
        background: rgba(255, 255, 255, 0.1);
        border-color: #667eea;
        transform: translateX(5px);
    }

    .quiz-option.correct {
        background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        border-color: #4facfe;
        color: #1a1a1a;
        font-weight: 600;
    }

    .quiz-option.incorrect {
        background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
        border-color: #fa709a;
        color: #1a1a1a;
        font-weight: 600;
    }

    .quiz-controls {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 2rem;
        padding-top: 2rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    .quiz-btn {
        padding: 0.75rem 2rem;
        border-radius: 25px;
        font-weight: 600;
        transition: all 0.3s ease;
        cursor: pointer;
        border: none;
    }

    .quiz-btn.primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
    }

    .quiz-btn.primary:hover:not(:disabled) {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
    }

    .quiz-btn.secondary {
        background: rgba(255, 255, 255, 0.1);
        color: white;
        border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .quiz-btn:disabled {
        opacity: 0.5;
        cursor: not-allowed;
    }

    .quiz-progress {
        font-weight: 600;
        color: #667eea;
    }

    /* Quiz Results */
    .quiz-results {
        text-align: center;
        padding: 2rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 15px;
        margin-top: 2rem;
    }

    .quiz-results.hidden {
        display: none;
    }

    .results-content h3 {
        font-size: 2rem;
        color: #667eea;
        margin-bottom: 1rem;
    }

    .score-display {
        font-size: 3rem;
        font-weight: 700;
        margin: 1rem 0;
        background: linear-gradient(135deg, #ffd89b 0%, #19547b 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .quiz-results p {
        font-size: 1.1rem;
        margin-bottom: 2rem;
    }

    .excellent { color: #4facfe; }
    .good { color: #ffd89b; }
    .needs-improvement { color: #fa709a; }

    /* Hierarchy Diagram */
    .hierarchy-diagram {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 2rem;
        margin: 2rem 0;
    }

    .ca-level {
        display: flex;
        gap: 3rem;
        justify-content: center;
    }

    .ca-node {
        padding: 1.5rem 2rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 15px;
        box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        transition: all 0.3s ease;
        text-align: center;
        min-width: 150px;
    }

    .ca-node:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 35px rgba(102, 126, 234, 0.4);
    }

    .ca-node span {
        font-weight: 600;
        color: white;
    }

    .root-ca .ca-node {
        background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
        font-size: 1.1rem;
    }

    .root-ca .ca-node span {
        color: #1a1a1a;
    }

    .hierarchy-lines {
        display: flex;
        gap: 4rem;
        position: relative;
        height: 30px;
    }

    .hierarchy-lines .line {
        width: 2px;
        height: 100%;
        background: linear-gradient(180deg, #667eea 0%, transparent 100%);
        position: relative;
    }

    .hierarchy-lines .line::before {
        content: '';
        position: absolute;
        bottom: 0;
        left: -20px;
        right: -20px;
        height: 2px;
        background: #667eea;
    }

    .cross-certification {
        margin-top: 2rem;
        padding: 1.5rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 15px;
        border-left: 4px solid #764ba2;
    }

    .cross-certification p {
        margin: 0;
        line-height: 1.6;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
        .hero-title {
            font-size: 2.5rem;
        }

        .hero-subtitle {
            font-size: 1.1rem;
        }

        .content-card {
            padding: 1.5rem;
        }

        .card-header h2 {
            font-size: 1.5rem;
        }

        .components-grid {
            grid-template-columns: 1fr;
        }

        .certificates-comparison {
            grid-template-columns: 1fr;
        }

        .process-flow {
            flex-direction: column;
        }

        .process-arrow {
            transform: rotate(90deg);
        }

        .security-features {
            grid-template-columns: 1fr;
        }

        .hierarchy-lines {
            flex-direction: column;
            gap: 1rem;
            height: auto;
        }

        .hierarchy-lines .line {
            height: 20px;
            width: 100%;
        }

        .hierarchy-lines .line::before {
            left: 0;
            right: 0;
            top: 50%;
            height: 2px;
            width: 100%;
        }

        .ca-level {
            flex-direction: column;
            gap: 1rem;
        }

        .quiz-controls {
            flex-direction: column;
            gap: 1rem;
        }
    }

    /* Animation utilities */
    .animate-slide-up {
        opacity: 0;
        transform: translateY(30px);
        transition: all 0.6s ease;
    }

    .animate-slide-up.visible {
        opacity: 1;
        transform: translateY(0);
    }

    /* Loading animation for interactive elements */
    @keyframes shimmer {
        0% {
            background-position: -200px 0;
        }
        100% {
            background-position: calc(200px + 100%) 0;
        }
    }

    .loading {
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
        background-size: 200px 100%;
        animation: shimmer 1.5s infinite;
    }